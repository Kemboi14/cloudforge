apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: cloudforge
  labels:
    app: users
    service: users-service
spec:
  type: ClusterIP
  ports:
  - port: 8002
    targetPort: 8002
    protocol: TCP
    name: http
  selector:
    app: users
